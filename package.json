{
  "name": "fromistargram",
  "version": "1.0.0",
  "private": true,
  "description": "Fromistargram monorepo workspace",
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev:frontend": "pnpm --filter @fromistargram/frontend dev",
    "dev:backend": "pnpm --filter @fromistargram/backend dev"
  }
}
